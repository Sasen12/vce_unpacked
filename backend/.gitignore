.venv/
__pycache__/
*.pyc

# Raw VCAA study design files are committed so the pipeline runs on a
# fresh clone. Only the derived output/*.json (the transformed,
# simplified dataset) is what the app ships.
!source_docs/*.docx
!source_docs/*.pdf
!source_docs/subjects.json
